# Frontend origin allowed to call this backend (CORS). Adjust if you host the
# frontend somewhere other than this backend's static route.
FRONTEND_ORIGIN=http://localhost:8000

# Optional: point at your own self-hosted jiosaavn-api instance instead of
# the public saavn.dev demo (recommended once past light personal use).
SAAVN_API_BASE=https://saavn.dev/api
